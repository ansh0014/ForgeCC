# ForgeCC
ForgeCC is a custom C++ compiler built in C++ using LLVM compiler infrastructure, designed to analyze C++ programs and automatically identify machine-learning workloads such as tensor operations and matrix computations, then optimize and offload suitable workloads to the laptop GPU.
